# TruthCircle - A2 Hosting 部署指南

本指南將協助您將 TruthCircle 部署到 A2 Hosting。

## 📋 前置準備

- A2 Hosting 帳號（共享主機、VPS 或專用主機方案）
- cPanel 存取權限
- SSH 存取權限（建議）
- FTP/SFTP 客戶端（如 FileZilla）

## 🚀 部署步驟

### 步驟 1: 上傳專案檔案

#### 方法 A: 使用 FTP/SFTP

1. 使用 FTP 客戶端連接到您的 A2 Hosting 帳號
2. 導航到您的應用程式目錄（例如：`/home/username/your-app/`）
3. 上傳整個專案資料夾，**排除以下內容**：
   - `node_modules/` 資料夾
   - `dist/` 或 `build/` 資料夾
   - `.env` 或 `.env.local` 檔案

#### 方法 B: 使用 cPanel File Manager

1. 登入 cPanel
2. 開啟 **File Manager**
3. 導航到目標目錄
4. 上傳專案 zip 檔案
5. 使用 File Manager 的 Extract 功能解壓縮

---

### 步驟 2: 設定 PostgreSQL 資料庫

#### 2.1 建立資料庫

1. 登入 **cPanel**
2. 找到 **PostgreSQL Databases**
3. 在 "Create New Database" 區域：
   - Database Name: 輸入 `truthcircle`
   - 點擊 **Create Database**
   - **記下完整的資料庫名稱**（例如：`username_truthcircle`）

#### 2.2 建立資料庫使用者

1. 在 "Add New User" 區域：
   - Username: 輸入使用者名稱（例如：`truthcircle_user`）
   - Password: 設定強密碼
   - 點擊 **Create User**
   - **記下完整的使用者名稱和密碼**

#### 2.3 授予權限

1. 在 "Add User To Database" 區域：
   - User: 選擇剛建立的使用者
   - Database: 選擇剛建立的資料庫
   - 點擊 **Add**
2. 在權限頁面，勾選 **ALL PRIVILEGES**
3. 點擊 **Make Changes**

#### 2.4 設定遠端存取

1. 在 cPanel 找到 **Remote PostgreSQL**
2. 在 "Access Hosts" 欄位輸入：`127.0.0.1`
3. 點擊 **Add Host**

---

### 步驟 3: 建置專案

#### 3.1 透過 SSH（建議）

```bash
# 登入 SSH
ssh username@your-domain.com

# 進入專案目錄
cd ~/your-app

# 安裝根目錄依賴
npm install

# 執行建置（會自動安裝 client 依賴並建置前端）
npm run build
```

#### 3.2 透過 cPanel Terminal

1. 在 cPanel 開啟 **Terminal**
2. 執行相同的命令

建置成功後，`server/dist` 資料夾會包含前端靜態檔案。

---

### 步驟 4: 設定 Node.js 應用程式

#### 4.1 建立應用程式

1. 登入 cPanel
2. 找到 **Setup Node.js App**
3. 點擊 **Create Application**

#### 4.2 基本設定

| 設定項目 | 值 |
|---------|-----|
| **Node.js version** | `18.x` 或更高版本 |
| **Application mode** | `Production` |
| **Application root** | `/home/username/your-app/server` ⚠️ |
| **Application URL** | 您的網域或子網域 |
| **Application startup file** | `index.js` |

⚠️ **重要：Application root 必須指向 `server` 子目錄！**

#### 4.3 設定環境變數

在 **Environment variables** 區域，加入以下變數：

**DATABASE_URL**
- 變數名稱：`DATABASE_URL`
- 變數值：`postgresql://使用者名稱:密碼@127.0.0.1:5432/資料庫名稱`

範例：
```
postgresql://username_truthcircle_user:MyPassword123@127.0.0.1:5432/username_truthcircle
```

**JWT_SECRET**
- 變數名稱：`JWT_SECRET`
- 變數值：任何隨機字串（例如：`your-random-secret-key-2024`）

**NODE_ENV**
- 變數名稱：`NODE_ENV`
- 變數值：`production`

#### 4.4 安裝依賴並啟動

1. 點擊 **Run NPM Install** 安裝 server 依賴
2. 點擊 **Start App** 啟動應用程式
3. 等待 10-20 秒讓應用完全啟動

---

### 步驟 5: 驗證部署

#### 5.1 檢查應用狀態

在 **Setup Node.js App** 中確認：
- 狀態顯示為 **Running** 或 **Started**
- 沒有錯誤訊息

#### 5.2 查看日誌

點擊應用程式旁的 **日誌圖示**，確認看到：
```
🚀 Server running on port 3001
⏳ Connecting to database...
✅ Database initialization successful
```

#### 5.3 測試網站

1. 開啟瀏覽器，訪問您的網域
2. 應該看到 TruthCircle 首頁和主題列表
3. 測試登入功能：
   - 使用者名稱：`admin`
   - 密碼：`admin`

---

## 🔧 常見問題排除

### 問題 1: 建置失敗（crypto.getRandomValues is not a function）

**原因：** Vite 5.x 與舊版 Node.js 不兼容

**解決方法：**
- 確認 Node.js 版本 >= 18.x
- 檢查 `client/package.json` 中 Vite 版本為 `^4.5.3`

### 問題 2: 資料庫連線失敗（no pg_hba.conf entry）

**原因：** `127.0.0.1` 未加入白名單

**解決方法：**
1. 確認 Remote PostgreSQL 中已加入 `127.0.0.1`
2. 在 Setup Node.js App 點擊 **Restart**
3. 等待 30 秒後重試

### 問題 3: 無法載入主題

**可能原因：**
- 環境變數 `DATABASE_URL` 未設定或格式錯誤
- 資料庫連線失敗

**解決方法：**
1. 檢查 `DATABASE_URL` 格式是否正確
2. 確認資料庫名稱、使用者名稱、密碼都正確
3. 查看應用程式日誌確認錯誤訊息
4. 重啟應用程式

### 問題 4: 404 錯誤或靜態檔案無法載入

**原因：** 前端未正確建置或 dist 資料夾不存在

**解決方法：**
```bash
cd ~/your-app
npm run build
# 確認 server/dist 資料夾存在
ls -la server/dist
```

---

## 🔐 安全性建議

### 1. 修改預設管理員密碼
首次登入後立即修改 `admin` 帳號密碼

### 2. 使用強 JWT_SECRET
不要使用簡單或預設的密鑰

### 3. 啟用 HTTPS
在 cPanel 的 SSL/TLS 區域設定 Let's Encrypt 免費 SSL 憑證

### 4. 定期備份
定期備份資料庫和應用程式檔案

---

## 📊 效能優化

### 1. 使用 Production Mode
確保 `NODE_ENV=production`

### 2. 資料庫連線池
應用程式已配置 PostgreSQL 連線池

### 3. 靜態資源快取
前端資源已自動快取

---

## 🔄 更新已部署的應用程式

當您在本地開發完成新功能或修正錯誤後，需要將更新部署到 A2 Hosting。

### 更新流程概覽

```
本地開發 → 測試 → 上傳更新 → 重新建置 → 重啟應用
```

---

### 方法 1: 使用 FTP/SFTP 更新（適合小幅修改）

#### 1.1 在本地測試

```bash
# 在本地確保程式碼正常運作
cd ~/truthcircle_web
npm run build
npm start
```

#### 1.2 上傳更新的檔案

使用 FTP 客戶端：
1. 連接到 A2 Hosting
2. 導航到專案目錄
3. **只上傳修改過的檔案**，例如：
   - 前端修改：上傳 `client/src/` 中的檔案
   - 後端修改：上傳 `server/index.js` 或其他 server 檔案
   - 新增依賴：上傳 `package.json` 或 `client/package.json`

#### 1.3 SSH 登入並重新建置

```bash
# SSH 登入
ssh username@your-domain.com

# 進入專案目錄
cd ~/your-app

# 如果有新增依賴，先安裝
npm install

# 重新建置（前端有修改時必須執行）
npm run build
```

#### 1.4 重啟應用

**方法 A: 透過 cPanel**
1. 進入 **Setup Node.js App**
2. 找到您的應用程式
3. 點擊 **Restart**

**方法 B: 透過 SSH**
```bash
# 使用 cPanel 提供的重啟命令（路徑可能不同）
touch ~/your-app/server/tmp/restart.txt
```

---

### 方法 2: 使用 Git 更新（建議，適合頻繁更新）

#### 2.1 初次設定 Git

如果還沒有設定 Git repository：

**在 A2 上：**
```bash
cd ~/your-app
git init
git remote add origin https://github.com/你的使用者名稱/truthcircle.git
git pull origin main
```

#### 2.2 更新流程

**本地開發機：**
```bash
# 本地開發並測試
git add .
git commit -m "描述您的更新"
git push origin main
```

**A2 Hosting 上：**
```bash
# SSH 登入
ssh username@your-domain.com

# 進入專案目錄
cd ~/your-app

# 拉取最新程式碼
git pull origin main

# 安裝新增的依賴（如果有）
npm install

# 重新建置
npm run build
```

#### 2.3 重啟應用

在 cPanel **Setup Node.js App** 點擊 **Restart**

---

### 不同類型更新的處理方式

#### 📝 只修改前端 UI（React 組件、CSS）

```bash
cd ~/your-app
git pull origin main  # 或上傳檔案
npm run build         # 必須重新建置
# 在 cPanel 重啟應用
```

#### 🔧 只修改後端邏輯（server/index.js）

```bash
cd ~/your-app
git pull origin main  # 或上傳檔案
# 不需要 npm run build
# 在 cPanel 重啟應用（必須重啟才會載入新程式碼）
```

#### 📦 新增或更新 npm 套件

```bash
cd ~/your-app
git pull origin main
npm install           # 安裝新依賴
npm run build        # 如果是前端依賴
cd server
npm install          # 如果是後端依賴
# 在 cPanel 重啟應用
```

#### 🗄️ 修改資料庫結構

```bash
# 1. 備份資料庫（重要！）
pg_dump -h 127.0.0.1 -U username -d dbname > backup.sql

# 2. 更新程式碼
cd ~/your-app
git pull origin main

# 3. 執行資料庫遷移（如果有）
# 根據您的程式碼調整

# 4. 重新建置並重啟
npm run build
# 在 cPanel 重啟應用
```

---

### 快速更新指令總結

**完整更新流程（所有修改）：**
```bash
cd ~/your-app
git pull origin main
npm install
npm run build
cd server && npm install && cd ..
# 然後在 cPanel 重啟
```

**前端快速更新：**
```bash
cd ~/your-app
git pull
npm run build
# 然後在 cPanel 重啟
```

**後端快速更新：**
```bash
cd ~/your-app/server
git pull
# 然後在 cPanel 重啟
```

---

### ⚠️ 更新時的注意事項

1. **總是先備份**
   - 在重大更新前備份資料庫
   - 保留舊版本的程式碼

2. **在本地測試**
   - 確保所有功能在本地正常運作
   - 檢查沒有編譯錯誤

3. **檢查依賴**
   - 如果 `package.json` 有變更，務必執行 `npm install`

4. **前端修改必須重新建置**
   - 任何 `client/` 目錄的修改都需要執行 `npm run build`
   - 建置時間約 30 秒到 2 分鐘

5. **後端修改必須重啟**
   - Node.js 不會自動重載程式碼
   - 必須在 cPanel 手動重啟應用

6. **查看日誌**
   - 更新後檢查應用程式日誌
   - 確認沒有錯誤訊息

---

### 🚨 更新失敗時的回滾

如果更新後出現問題：

```bash
# 回滾到上一個版本
cd ~/your-app
git log --oneline  # 查看歷史記錄
git reset --hard HEAD~1  # 回到上一個版本
npm run build
# 在 cPanel 重啟
```

或從備份還原資料庫：
```bash
psql -h 127.0.0.1 -U username -d dbname < backup.sql
```



---

## 📞 獲取協助

如遇到問題：

1. **查看日誌**：Setup Node.js App → 日誌圖示
2. **檢查錯誤日誌**：cPanel → Error Logs
3. **聯絡 A2 Support**：如果是主機相關問題
4. **查看專案文檔**：README.md

---

## ✅ 部署檢查清單

- [ ] 專案檔案已上傳到 A2
- [ ] PostgreSQL 資料庫已建立
- [ ] 資料庫使用者已建立並授權
- [ ] `127.0.0.1` 已加入 Remote PostgreSQL 白名單
- [ ] 已執行 `npm run build` 建置專案
- [ ] Node.js App 已設定（Application root 指向 server）
- [ ] 環境變數已設定（DATABASE_URL, JWT_SECRET, NODE_ENV）
- [ ] 應用程式已啟動且狀態為 Running
- [ ] 網站可正常訪問
- [ ] 登入功能正常運作
- [ ] 已修改預設管理員密碼

---

**恭喜！您的 TruthCircle 應用程式已成功部署到 A2 Hosting！** 🎉
